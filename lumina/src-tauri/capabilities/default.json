{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability by default",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
